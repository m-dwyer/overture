# build artifacts
build/
dist/
*.so
.DS_Store

# Local developer env (MOVE_HOST etc.)
.env

# Generated manual guides (open locally; the committed .md is the source of truth).
# See README → Docs for how to generate and open them.
docs/generated/*.html
