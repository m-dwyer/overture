# TODO

- Define a small Overture UI surface/design-language layer for modal stack behavior: consistent "back one layer" semantics, list/browser chrome, empty/error states, and shortcut hints. Use the Sound Edit preset browser's Menu-to-parent behavior as an input, then replace one-off handlers with a shared pattern.
- Consolidate shortcut handling into an explicit shortcut registry/router. Separate always-available global shortcuts from modal/browser shortcuts and page-local shortcuts, define precedence rules, and make modifier consumption visible so combos like Capture+jog cannot accidentally leak into pad playback or leave state ambiguous.
- Promote modal input ownership into a shared surface contract: opening a modal can claim pads/buttons, closing it must release any host pad block, clear consumed modifiers, and repush the pad map synchronously when needed. The Sound Edit preset browser exposed this via Copy+jog and text-entry pad typing; avoid feature-local cleanup code long term.
- Improve Sound Edit preset feedback for "silent but loaded" presets. Loading a preset with output controls such as `volume=0` can look like broken pad dispatch even when note input is working; consider a status warning, exclusion/option for output-level params, or clearer parameter labeling for module-specific volume controls.
- Build a dedicated preset-name keyboard surface for preset save flows. It may reuse low-level shared text-entry primitives, but should own preset-specific modal lifecycle, pad typing, name validation/sanitization preview, duplicate/overwrite handling, save/cancel status, and return-to-Sound-Edit behavior instead of inheriting generic text-entry defaults.
